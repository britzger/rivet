#ifndef RIVETGUN_RivetGun_HH
#define RIVETGUN_RivetGun_HH

#include <typeinfo>
#include <set>
#include <map>
#include <string>
#include <vector>
#include <stdexcept>
#include <iostream>
#include <iomanip>
#include <sstream>
#include <list>

/**
 * This is the main namespace in which all Rivet classes are defined.
 */
namespace Rivet {

  using std::set;
  using std::map;
  using std::multimap;
  using std::type_info;
  using std::string;
  using std::less;
  using std::vector;
  using std::list;
  using std::pair;
  using std::make_pair;
  using std::runtime_error;
  using std::exception;
  using std::min;
  using std::stringstream;
  using std::max;
  using std::ostream;
  using std::istream;
  using std::cout;
  using std::cin;
  using std::cerr;
  using std::setw;
  using std::endl;

}

#endif
